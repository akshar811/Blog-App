<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="../signup.css" />
    <title>Create Post</title>
  </head>
  <body>
    <%-include("navbar") %>

    <div class="content">
      <h1>Create New Post</h1> <br> <br>
      <form action="/blog/createBlog" method="POST">
        <div class="field">
          <input type="text" name="title" id="title" required  placeholder="title"/>
        </div>

        <div class="field">
          <textarea name="content" id="content" placeholder="content" required></textarea>
        </div>

        <div class="field">
          <input type="url" name="img" placeholder="image">
        </div>

        <div class="field">
          <button type="submit" class="btn">Submit</button>
        </div> <br> <br> <br> <br>
        <a href="/user/Homepage" class="btnposts">Back to Posts</a>
      </form>
    </div>
  </body>
</html>
